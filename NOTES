CTRS VERIFIED BUGS

CTRS NEEDED FEATURES
 - Permabuffs integration (update permabuffs and possibly make the generalized command) [no longer a priority]

CTRS OPTIONAL FEATURES

DATABASE ACCESS RULES
 - Every change to the total credit count should go through the REST /transaction endpoint. The tier field should never be changed manually, unless tier-locking a user.